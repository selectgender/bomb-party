import { CommandArgument, CommandDefinition } from "@rbxts/cmdr";

export = identity<CommandDefinition>({
	Name: "startBorder",
	Aliases: ["stb"],
	Description: "Starts the border closin! Intended for core gameplay",
	Group: "Admin",
	Args: [],
});
