import { CommandArgument, CommandDefinition } from "@rbxts/cmdr";

export = identity<CommandDefinition>({
	Name: "stopBorder",
	Aliases: ["spb"],
	Description: "Stops the border closin! Intended for core gameplay",
	Group: "Admin",
	Args: [],
});
