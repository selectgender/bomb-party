import { CommandArgument, CommandDefinition } from "@rbxts/cmdr"

export = identity<CommandDefinition>({
  Name: "bombRain",
  Aliases: ["br"],
  Description: "Creates a bomb rain, intended for core gameplay",
  Group: "Admin",
  Args: []
})
